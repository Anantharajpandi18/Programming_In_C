#include<stdio.h>

// Main function
int main(){
	// Declare a variable (say a) and assign 5 to it.
	int a = 5;

	// Now use pre-increment on 'a' and assign it to 'b'.
	int b = ++a;

	// Print 'a' and 'b'
	printf("a = %d\nb = %d\n", a, b);

	return 0;

}
